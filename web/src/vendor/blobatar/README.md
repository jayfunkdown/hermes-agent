# Vendored blobatar@2.0.0 SVG renderer (MIT).
# Source: https://www.npmjs.com/package/blobatar (dist/blob.js)
# Desktop uses the same published package; web vendors the self-contained
# entry so the dashboard build does not depend on a workspace lockfile sync.
